# BinaInsight Module

This module owns the operational BinaInsight workflow inside `app.binahub.id`:

- assessment form
- answer submission
- scoring
- result page
- report generation
- admin visibility

The public explanation page can remain in `website-prod`.
